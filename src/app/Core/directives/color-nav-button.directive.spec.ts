import { ColorNavButtonDirective } from './color-nav-button.directive';

describe('ColorNavButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorNavButtonDirective();
    expect(directive).toBeTruthy();
  });
});
